<%= form_for [@domain, @mail_alias] do |f| %>
  <% if @mail_alias.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mail_alias.errors.count, "error") %> prohibited this mail_alias from being saved:</h2>

      <ul>
      <% @mail_alias.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :domain_id %><br />
    <%= collection_select :mail_alias, :domain_id, Domain.all, :id, :domain %>
  </div>
  <div class="field">
    <%= f.label :source %><br />
    <%= f.text_field :source %><span>@<%= @domain.domain %></span>
  </div>
  <div class="field">
    <%= f.label :destination %><br />
    <%= f.text_field :destination %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
